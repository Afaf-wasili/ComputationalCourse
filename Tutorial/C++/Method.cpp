vector<int> numbers;
/*
void Method(){
  
  numbers.push_back(5); //adding element into group
  numbers.push_back(6);
  numbers.push_back(7);

  for ( int i = 0; i < numbers.size(); i++){
    std::cout<<"numbers= "<< numbers[i] <<std::endl;

  }
}
*/




void Method(){

  numbers.push_back(5); //adding element into group
  numbers.push_back(6);
  numbers.push_back(7);


  // numbers.erase(numbers.begin() + 1); //will remove 6

   for ( int i = 0; i < numbers.size(); i++){
     //   if ( i == 1) continue; //skip 
     std::cout<<"numbers= "<< numbers[i] <<std::endl;

   }}
//index 0 for 5
//index 1 for 6
//index 2 for 7
 
