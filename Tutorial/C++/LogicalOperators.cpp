/*
void LogicalOperators(){
  int a = 10;
  int b = 5;
  
  // std::cout<<"a: "<< a << " b: "<< b <<std::endl; 
  std::cout<<"a greater than b= "<< (a > b) <<std::endl;
  std::cout<<"a < b= "<< (a < b) <<std::endl;
  std::cout<<"a is equal b "<< (a == b) <<std::endl;
  std::cout<<"a is not equal to b "<< (a != b) <<std::endl;
    
}
*/


void LogicalOperators(){
  int a = 100;
  int b = 0;
  //  if( (a && b > 0) ){
  if( (a || b > 0) ){
  std::cout<< "the number is a or b" <<std::endl; 
  }}



/*
void LogicalOperators(){

  int num = 2;
  if ( num  % 2 == 0)  // if the number is even the result of num  % 2 == 0 if the number is odd the result 1
    { std::cout<< " the number is even"<<std::endl;
    }}

*/






/*
  | Meaning     | Example    |    |     |   |     |
| -------- | ------------ | ---------- | -- | --- | - | --- |
| `>`      | greater than | `a > b`    |    |     |   |     |
| `<`      | less than    | `a < b`    |    |     |   |     |
| `==`     | equal        | `a == b`   |    |     |   |     |
| `!=`     | not equal    | `a != b`   |    |     |   |     |
| `&&`     | AND          | `(x && y)` |    |     |   |     |
   `||`      OR             `(x || y)`
*/
