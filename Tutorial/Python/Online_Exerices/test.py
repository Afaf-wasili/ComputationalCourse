import numpy as np

# Define the arrays using np.array
a = np.array([10, 20, 30])
b = np.array([1, 2, 0])

# Initialize an empty NumPy array for the result
#c = np.array([0] * len(a))

# Perform the addition using a for loop
#c = []
for i in range(len(a)):
    c[i] = a[i] + b[i]

# Print the result
print(c[i])
